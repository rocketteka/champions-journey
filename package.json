{
  "name": "champions-journey",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-app-engine.mjs && vite build",
    "preview": "vite preview",
    "start": "node index.js",
    "typecheck": "tsc -b --noEmit",
    "firebase:rules": "npx firebase-tools deploy --only firestore:rules",
    "firebase:seed": "node scripts/seed-test-accounts.mjs",
    "firebase:bootstrap": "node scripts/bootstrap-firestore.mjs"
  },
  "dependencies": {
    "firebase": "^11.6.0",
    "serve": "^14.2.6",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "devDependencies": {}
}
